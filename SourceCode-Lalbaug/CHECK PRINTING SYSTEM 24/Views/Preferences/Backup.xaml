<UserControl x:Class="CPS.Views.Preferences.Backup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="110" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <Label Content="Database Backup Path" Target="{Binding ElementName=txtBackUpPath}" />
                    <TextBox Name="txtBackUpPath"></TextBox>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <Button Content="Browse" Name="btnBackupPath" Click="btnBackupPath_MouseDown" Margin="0,26,0,0" />
                </StackPanel>
            </Grid>
            
            <Button Content="Save" Name="btnSave" Margin="15" HorizontalAlignment="Center" Click="btnSave_Click" />
        </StackPanel>
    </Grid>
</UserControl>
